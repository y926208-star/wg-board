version: v2.0

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: docker
    steps:
      - checkout: self
      - run: |
          echo "=== 部署WG看板 ==="
          ls -la
          echo "文件已就绪，仓库内容可通过工蜂 Raw 访问"
          echo "访问地址: https://git.woa.com/yongtaoyang/wegame/raw/main/index.html"
